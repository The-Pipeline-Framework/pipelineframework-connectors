package org.pipelineframework.connector.graphql.smallrye;

/** Static authority classification for an application-owned persisted operation. */
public enum GraphQlOperationKind {
    QUERY,
    MUTATION
}
